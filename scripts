<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QUO BOOST | Scripts Hub</title>
    <!-- Fuentes Futuristas -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;500;700&family=Inter:wght@400;600&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --bg-deep: #030305;
            --bg-panel: rgba(20, 20, 25, 0.6);
            --primary: #ffffff;
            --accent-cyan: #00f2ea;
            --accent-purple: #7000ff;
            --accent-glow: rgba(0, 242, 234, 0.5);
            --grid-color: rgba(255, 255, 255, 0.03);
            --border-glow: rgba(255, 255, 255, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-font-smoothing: antialiased;
        }

        body {
            background-color: var(--bg-deep);
            color: var(--primary);
            font-family: 'Inter', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            overflow-x: hidden;
            perspective: 1000px;
        }

        /* --- BACKGROUND EFFECTS --- */
        .bg-grid {
            position: fixed;
            top: 0;
            left: 0;
            width: 200vw;
            height: 200vh;
            background-image: 
                linear-gradient(var(--grid-color) 1px, transparent 1px),
                linear-gradient(90deg, var(--grid-color) 1px, transparent 1px);
            background-size: 50px 50px;
            transform: perspective(500px) rotateX(60deg) translateY(-100px) translateZ(-200px);
            animation: gridMove 20s linear infinite;
            z-index: -2;
            pointer-events: none;
        }

        .bg-glow {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 80vw;
            height: 80vh;
            background: radial-gradient(circle, rgba(112, 0, 255, 0.15) 0%, rgba(0, 0, 0, 0) 70%);
            z-index: -1;
            pointer-events: none;
        }

        @keyframes gridMove {
            0% { transform: perspective(500px) rotateX(60deg) translateY(0) translateZ(-200px); }
            100% { transform: perspective(500px) rotateX(60deg) translateY(50px) translateZ(-200px); }
        }

        /* --- HEADER & HERO --- */
        header {
            text-align: center;
            margin: 80px 0 60px;
            z-index: 10;
            position: relative;
        }

        h1 {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 4rem;
            font-weight: 700;
            letter-spacing: -2px;
            margin-bottom: 40px;
            background: linear-gradient(135deg, #fff 30%, #a5a5a5 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 0 0 30px rgba(255, 255, 255, 0.1);
            animation: fadeInDown 1s ease-out;
        }

        .cta-container {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
            animation: fadeInUp 1s ease-out 0.2s backwards;
        }

        /* --- 3D BUTTONS --- */
        .btn-3d {
            position: relative;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            font-size: 0.95rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #fff;
            text-decoration: none;
            padding: 16px 36px;
            border-radius: 8px;
            transition: all 0.3s cubic-bezier(0.2, 0.8, 0.2, 1);
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.1);
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(5px);
        }

        .btn-neon {
            background: linear-gradient(90deg, transparent, rgba(0, 242, 234, 0.1), transparent);
            background-size: 200% 100%;
            border-color: var(--accent-cyan);
            box-shadow: 0 0 15px rgba(0, 242, 234, 0.2);
            animation: shimmer 3s infinite linear;
        }

        .btn-neon:hover {
            box-shadow: 0 0 30px rgba(0, 242, 234, 0.6), inset 0 0 10px rgba(0, 242, 234, 0.4);
            transform: translateY(-2px) scale(1.02);
            text-shadow: 0 0 8px rgba(255, 255, 255, 0.8);
            border-color: #fff;
        }

        .btn-outline {
            border-color: rgba(255, 255, 255, 0.2);
        }

        .btn-outline:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: #fff;
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.1);
        }

        @keyframes shimmer {
            0% { background-position: 100% 0; }
            100% { background-position: -100% 0; }
        }

        /* --- 3D CARD CONTAINER --- */
        .grid-wrapper {
            width: 100%;
            max-width: 1200px;
            display: flex; /* Changed to flex for centering single item perfectly */
            justify-content: center; 
            gap: 40px;
            padding: 20px;
            perspective: 2000px; /* Essential for 3D effect */
            z-index: 5;
        }

        /* --- THE 3D CARD --- */
        .card-3d-wrap {
            position: relative;
            width: 380px;
            height: 500px; /* Taller for better proportion */
            transform-style: preserve-3d;
            transform: rotateX(0deg) rotateY(0deg);
            transition: transform 0.1s ease-out; /* Super smooth movement */
            cursor: pointer;
            animation: fadeInUp 1s ease-out 0.4s backwards;
        }

        .card-content-box {
            position: absolute;
            inset: 0;
            background: var(--bg-panel);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            overflow: hidden;
            backdrop-filter: blur(20px);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
            
            /* Inner lighting effect */
            background: linear-gradient(
                135deg, 
                rgba(255, 255, 255, 0.05) 0%, 
                rgba(0, 0, 0, 0.8) 100%
            );
        }

        /* Dynamic reflection overlay */
        .card-shine {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(
                circle at var(--mouse-x) var(--mouse-y), 
                rgba(255, 255, 255, 0.2), 
                transparent 50%
            );
            opacity: 0;
            transition: opacity 0.3s;
            pointer-events: none;
            z-index: 10;
            mix-blend-mode: overlay;
        }

        .card-3d-wrap:hover .card-shine {
            opacity: 1;
        }

        .card-3d-wrap:hover .card-content-box {
            border-color: var(--accent-cyan);
            box-shadow: 0 0 30px rgba(0, 242, 234, 0.2);
        }

        .card-image {
            width: 100%;
            height: 60%;
            object-fit: cover;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            transition: transform 0.4s;
        }

        .card-3d-wrap:hover .card-image {
            transform: scale(1.05);
        }

        .card-info {
            padding: 25px;
            position: relative;
            z-index: 2;
        }

        .tag-badge {
            display: inline-block;
            padding: 4px 12px;
            background: rgba(0, 242, 234, 0.1);
            color: var(--accent-cyan);
            border: 1px solid rgba(0, 242, 234, 0.3);
            border-radius: 100px;
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 15px;
            box-shadow: 0 0 10px rgba(0, 242, 234, 0.1);
        }

        .card-title-text {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1.5rem;
            line-height: 1.3;
            font-weight: 700;
            margin-bottom: 10px;
            color: #fff;
        }

        .click-hint {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 0.85rem;
            color: #888;
            margin-top: 15px;
            font-weight: 500;
        }

        .click-hint svg {
            transition: transform 0.3s;
        }

        .card-3d-wrap:hover .click-hint svg {
            transform: translateX(5px);
            color: var(--accent-cyan);
        }

        /* --- MODAL DESIGN --- */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.85);
            backdrop-filter: blur(15px);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 100;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .modal-overlay.active {
            opacity: 1;
            pointer-events: all;
        }

        .modal-box {
            width: 90%;
            max-width: 850px;
            max-height: 85vh;
            background: #0a0a0c;
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            transform: scale(0.9) translateY(30px);
            transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 0 100px rgba(0, 0, 0, 0.8), 0 0 0 1px rgba(255, 255, 255, 0.05);
        }

        .modal-overlay.active .modal-box {
            transform: scale(1) translateY(0);
        }

        .modal-header {
            padding: 24px 32px;
            background: rgba(255, 255, 255, 0.02);
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .modal-h2 {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1.2rem;
            color: #fff;
            letter-spacing: 0.5px;
        }

        .close-icon {
            cursor: pointer;
            padding: 8px;
            border-radius: 50%;
            color: #888;
            background: transparent;
            border: none;
            transition: all 0.2s;
        }

        .close-icon:hover {
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
            transform: rotate(90deg);
        }

        .modal-content {
            padding: 32px;
            overflow-y: auto;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #050505; }
        ::-webkit-scrollbar-thumb { background: #333; border-radius: 3px; }
        ::-webkit-scrollbar-thumb:hover { background: var(--accent-cyan); }

        /* --- CODE BLOCKS --- */
        .code-container {
            margin-bottom: 24px;
            animation: fadeIn 0.5s ease-out;
        }

        .label {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: var(--accent-cyan);
            margin-bottom: 10px;
            display: block;
            font-weight: 700;
        }

        .code-box-wrapper {
            position: relative;
        }

        .code-snippet {
            background: #111;
            border: 1px solid #222;
            border-radius: 8px;
            padding: 20px;
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.85rem;
            line-height: 1.7;
            color: #dcdcdc;
            overflow-x: auto;
            white-space: pre-wrap;
            transition: border-color 0.3s;
        }

        .code-snippet:hover {
            border-color: #444;
        }

        .btn-copy {
            position: absolute;
            top: 10px;
            right: 10px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: #aaa;
            padding: 6px 14px;
            border-radius: 6px;
            font-size: 0.7rem;
            cursor: pointer;
            font-family: 'Inter', sans-serif;
            font-weight: 600;
            transition: all 0.2s;
        }

        .btn-copy:hover {
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
        }

        .btn-copy.success {
            background: #00f2ea;
            color: #000;
            border-color: #00f2ea;
        }

        /* --- ANIMATIONS --- */
        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* Mobile Adjustments */
        @media (max-width: 768px) {
            h1 { font-size: 2.5rem; }
            .card-3d-wrap { width: 100%; height: auto; min-height: 400px; }
            .grid-wrapper { padding: 10px; }
        }
    </style>
</head>
<body>

    <!-- Dynamic Background -->
    <div class="bg-grid"></div>
    <div class="bg-glow"></div>

    <header>
        <h1>SCRIPTS USED IN VIDEOS</h1>
        <div class="cta-container">
            <a href="https://quoboost.com/download" target="_blank" class="btn-3d btn-neon">
                Download Quo Boost App
            </a>
            <a href="/pricing" class="btn-3d btn-outline">
                Unlock Full Performance
            </a>
        </div>
    </header>

    <main class="grid-wrapper">
        
        <!-- 3D Card Interactive Element -->
        <div class="card-3d-wrap" id="card3d" onclick="openModal()">
            <div class="card-content-box">
                <!-- Shine effect layer -->
                <div class="card-shine"></div>
                
                <img src="assets/itried3ways.png" alt="Optimization Guide" class="card-image">
                
                <div class="card-info">
                    <span class="tag-badge">System Tweak</span>
                    <h2 class="card-title-text">I Tried 3 ways to Optimize my 500$ PC in 2026</h2>
                    <div class="click-hint">
                        View Commands 
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
                    </div>
                </div>
            </div>
        </div>

    </main>

    <!-- Modal Popup -->
    <div class="modal-overlay" id="scriptModal">
        <div class="modal-box">
            <div class="modal-header">
                <h2 class="modal-h2">Optimization Configuration</h2>
                <button class="close-icon" onclick="closeModal()">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
                </button>
            </div>
            
            <div class="modal-content">
                
                <!-- Code Block 1 -->
                <div class="code-container">
                    <span class="label">Primary Optimization</span>
                    <div class="code-box-wrapper">
                        <div class="code-snippet" id="snip1">bcdedit /set Disabledynamictick yes
bcdedit /set useplatformtick yes</div>
                        <button class="btn-copy" onclick="copyText('snip1', this)">COPY</button>
                    </div>
                </div>

                <!-- Code Block 2 -->
                <div class="code-container">
                    <span class="label">Menu Animations (Regedit)</span>
                    <div class="code-box-wrapper">
                        <div class="code-snippet" id="snip2">Path: HKEY_CURRENT_USER\Control Panel\Desktop

MenuShowDelay: 0
WaitToKillAppTimeOut: 1000</div>
                        <button class="btn-copy" onclick="copyText('snip2', this)">COPY</button>
                    </div>
                </div>

                <!-- Code Block 3 -->
                <div class="code-container">
                    <span class="label">IO Priorities</span>
                    <div class="code-box-wrapper">
                        <div class="code-snippet" id="snip3">Path: HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Reliability

TimeStampInterval = 1
IoPriority = 3</div>
                        <button class="btn-copy" onclick="copyText('snip3', this)">COPY</button>
                    </div>
                </div>

                <!-- Code Block 4 -->
                <div class="code-container">
                    <span class="label">Memory Mapping (CMD Admin)</span>
                    <div class="code-box-wrapper">
                        <div class="code-snippet" id="snip4">bcdedit /set configaccesspolicy Default
bcdedit /set MSI Default
bcdedit /set usephysicaldestination No
bcdedit /set usefirmwarepcisettings No</div>
                        <button class="btn-copy" onclick="copyText('snip4', this)">COPY</button>
                    </div>
                </div>

                <!-- Code Block 5 -->
                <div class="code-container">
                    <span class="label">Shortcuts</span>
                    <div class="code-box-wrapper">
                        <div class="code-snippet" id="snip5">gpedit.msc
sysdm.cpl</div>
                        <button class="btn-copy" onclick="copyText('snip5', this)">COPY</button>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <script>
        // --- 3D TILT EFFECT LOGIC ---
        const card = document.getElementById('card3d');
        const container = document.querySelector('.grid-wrapper');

        // Logic to rotate card based on mouse position
        container.addEventListener('mousemove', (e) => {
            const rect = card.getBoundingClientRect();
            const x = e.clientX - rect.left; // Mouse position inside the element
            const y = e.clientY - rect.top;

            // Calculate rotation (center is 0,0)
            // Divide by factor to reduce intensity (higher number = less rotation)
            const xRotation = -((y - rect.height / 2) / 20); 
            const yRotation = (x - rect.width / 2) / 20;

            // Update CSS variables for shine effect
            card.style.setProperty('--mouse-x', `${x}px`);
            card.style.setProperty('--mouse-y', `${y}px`);

            // Apply rotation
            card.style.transform = `rotateX(${xRotation}deg) rotateY(${yRotation}deg) scale(1.02)`;
        });

        // Reset on mouse leave
        container.addEventListener('mouseleave', () => {
            card.style.transform = 'rotateX(0) rotateY(0) scale(1)';
        });

        // --- MODAL LOGIC ---
        const modal = document.getElementById('scriptModal');

        function openModal() {
            modal.classList.add('active');
            document.body.style.overflow = 'hidden';
        }

        function closeModal() {
            modal.classList.remove('active');
            document.body.style.overflow = '';
        }

        modal.addEventListener('click', (e) => {
            if (e.target === modal) closeModal();
        });

        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && modal.classList.contains('active')) closeModal();
        });

        // --- COPY TO CLIPBOARD LOGIC ---
        async function copyText(elementId, btn) {
            const text = document.getElementById(elementId).innerText;
            try {
                await navigator.clipboard.writeText(text);
                
                const originalText = btn.innerText;
                btn.innerText = 'COPIED!';
                btn.classList.add('success');
                
                setTimeout(() => {
                    btn.innerText = originalText;
                    btn.classList.remove('success');
                }, 2000);
            } catch (err) {
                console.error('Error copying text:', err);
            }
        }
    </script>
</body>
</html>
